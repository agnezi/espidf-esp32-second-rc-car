#ifndef SECRETS_H
#define SECRETS_H

// MAC address of the allowed ESP-NOW transmitter (your RC car controller).
// To find it: flash the transmitter and read the MAC from its boot log,
// or run esp_wifi_get_mac(WIFI_IF_STA, mac) on the transmitter.
//
// Copy this file to secrets.h and replace with your transmitter's MAC:
#define TRANSMITTER_MAC {0x00, 0x00, 0x00, 0x00, 0x00, 0x00}

#endif
